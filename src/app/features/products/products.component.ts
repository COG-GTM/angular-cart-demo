import { Component } from '@angular/core';

/**
 * PLACEHOLDER — to be implemented by the Products feature session.
 * Port from AngularJS: client/app/products/products.controller.js + products.html
 * Should load products via ProductService (GET /api/products), support search
 * filtering + name ordering, add/remove to cart via CartService, and live-sync
 * via SocketService (syncUpdates/unsyncUpdates on 'product').
 */
@Component({
  selector: 'app-products',
  standalone: true,
  template: `<div class="container"><p>Products — coming soon.</p></div>`
})
export class ProductsComponent {}
